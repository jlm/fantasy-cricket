<li>
  <span class="name"><%= link_to user.name, user %></span>
  <span class="timestamp">
    Updated <%= time_ago_in_words(user.updated_at) %> ago.
  </span>
  <% if current_user.id == user.id %>
    <span><%= link_to 'Edit', edit_user_path(user), class: "btn btn-default" %></span>
    <span><%= link_to 'Destroy', user, method: :delete,
  	  data: { confirm: 'Destroy this user and their team?' }, class: "btn btn-danger" %></span>
  <% end %>    
  <br>
</li>
