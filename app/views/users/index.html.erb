<h1>Listing users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
    
  <% end %>
</ul>
<br>

<%= will_paginate %>

<%= link_to 'New User', new_user_path %>
